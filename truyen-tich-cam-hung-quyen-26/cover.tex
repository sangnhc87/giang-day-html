%============================================================
% TRANG BIA - QUYEN XXVI
%============================================================
\definecolor{seriesaccent}{HTML}{B56576}
\begin{titlepage}
\thispagestyle{empty}
\pagecolor{seriesaccent!7}

\begin{tikzpicture}[remember picture, overlay]
  \fill[white, opacity=0.62, rounded corners=20pt]
    ($(current page.north west)+(1.5cm,-1.5cm)$) rectangle
    ($(current page.south east)+(-1.5cm,1.5cm)$);

  \draw[clayrose!80!black, line width=3pt, rounded corners=15pt]
    ($(current page.north west)+(1.8cm,-1.8cm)$) rectangle
    ($(current page.south east)+(-1.8cm,1.8cm)$);

  \draw[clayrose, line width=1pt, rounded corners=12pt]
    ($(current page.north west)+(2cm,-2cm)$) rectangle
    ($(current page.south east)+(-2cm,2cm)$);

  \foreach \x/\y/\xsign/\ysign in {%
    north west/1.8cm/1/-1,
    north east/1.8cm/-1/-1,
    south west/1.8cm/1/1,
    south east/1.8cm/-1/1%
  } {
    \draw[clayrose!80!black, line width=1.5pt]
      ($(current page.\x) + (\xsign*0.8cm, \ysign*2.8cm)$) --
      ($(current page.\x) + (\xsign*2.8cm, \ysign*2.8cm)$) --
      ($(current page.\x) + (\xsign*2.8cm, \ysign*0.8cm)$);

    \fill[clayrose!60!white] ($(current page.\x) + (\xsign*2cm, \ysign*2cm)$) circle (3pt);
  }

  \node[anchor=center] at ($(current page.center)-(0, 2cm)$) {
    \begin{tikzpicture}
      \draw[deepsea!20, line width=0.5pt] (0,0) circle (5cm);
      \draw[clayrose!45, line width=1pt] (0,0) circle (4.8cm);
    \end{tikzpicture}
  };
\end{tikzpicture}

\vspace*{2cm}

\begin{center}
  {\color{clayrose!90!black}\large$\star\;\star\;\star$}\\[0.3cm]
  {\fontsize{14}{16}\selectfont\color{deepsea!85!black}\scshape \textbf{Những đối thoại trong một mái nhà}}\\[1.4cm]

  \begin{tcolorbox}[
    width=6.5cm,
    colback=clayrose!20,
    colframe=clayrose!80!black,
    arc=4pt,
    boxrule=1.5pt,
    halign=center
  ]
    {\fontsize{18}{22}\selectfont\bfseries\color{inkdark!80!black}
     QUYỂN XXVI}
  \end{tcolorbox}
  \vspace{0.8cm}

  {\fontsize{32}{38}\selectfont\bfseries\color{inkdark!80!black}
    Phụ Huynh}\\[0.35cm]
  {\fontsize{32}{38}\selectfont\bfseries\color{clayrose!85!black}
    Và Con Cái}\\[0.4cm]

  {\fontsize{16}{20}\selectfont\color{clayrose!90!black}\itshape
    100 màn đối thoại trong gia đình\\
    Nhiều yêu thương, nhiều hiểu lầm, và nhiều điều cần học lại}\\[1cm]

  {\color{deepsea!60}\rule{6cm}{1pt}}\\[5pt]
  {\color{clayrose!80!black}\large$\diamond$\hspace{1cm}$\diamond$\hspace{1cm}$\diamond$}\\[5pt]
  {\color{deepsea!60}\rule{6cm}{1pt}}\\[1.5cm]

  {\normalsize\itshape\color{deepsea!90!black}
   ``Có những nhà không thiếu tình thương.\\
   Chỉ thiếu một cách nói để tình thương đi tới nơi.''}

  \vfill

  {\fontsize{16}{20}\selectfont\bfseries\color{deepsea!90!black}
    Sưu tầm \& Tổng hợp: \textbf{Nguyễn Văn Sang}}\\[0.4cm]

  {\small\color{gray} Bản mềm phát hành --- Năm 2026}\\[0.5cm]
\end{center}

\afterpage{\nopagecolor}
\end{titlepage}