\begin{titlepage}
\thispagestyle{empty}
\begin{tikzpicture}[remember picture, overlay]
  % Background gradient blocks
  \fill[truyenblue] (current page.north west) rectangle (current page.south east);
  \fill[truyenblue!80!black] (current page.north west)
    [rounded corners=0pt] --
    ([xshift=0pt,yshift=-12cm]current page.north east) --
    (current page.south east) --
    (current page.south west) -- cycle;

  % Top decorative gold bar
  \fill[truyengold] ([yshift=-3.5cm]current page.north west)
    rectangle ([yshift=-3.7cm]current page.north east);

  % Bottom decorative bar
  \fill[truyengold!70] ([yshift=2.8cm]current page.south west)
    rectangle ([yshift=2.6cm]current page.south east);

  % Series stripe
  \node[anchor=north, text=white, font=\large\itshape] at
    ([yshift=-1.2cm]current page.north) {%
      \textbf{Truyện Tích Cảm Hứng Song Ngữ} ---
      \textit{Vòng Quay Cuộc Đời}%
    };

  % Volume badge
  \node[
    fill=truyengold, text=truyenblue,
    font=\large\bfseries,
    minimum width=3.2cm, minimum height=1.1cm,
    rounded corners=6pt
  ] at ([yshift=-3.1cm]current page.north) {QUYỂN VI};

  % Main title
  \node[
    anchor=center,
    text=white,
    font=\fontsize{52}{56}\selectfont\bfseries\scshape
  ] at ([yshift=2.5cm]current page.center) {Vòng Quay};

  \node[
    anchor=center,
    text=truyengold,
    font=\fontsize{52}{56}\selectfont\bfseries\scshape
  ] at ([yshift=0.5cm]current page.center) {Cuộc Đời};

  % Divider
  \draw[truyengold, line width=2pt]
    ([xshift=-5cm,yshift=-0.8cm]current page.center) --
    ([xshift=5cm,yshift=-0.8cm]current page.center);

  % Subtitle / themes
  \node[
    anchor=center,
    text=white,
    font=\large\itshape
  ] at ([yshift=-1.6cm]current page.center) {%
    Nhân Quả $\cdot$ Bản Sắc $\cdot$ Buông Bỏ $\cdot$ Người Thầy Vô Hình%
  };

  % Decorative diamonds
  \node[text=truyengold, font=\Huge] at ([xshift=-6cm,yshift=2.5cm]current page.center) {$\lozenge$};
  \node[text=truyengold, font=\Huge] at ([xshift=6cm,yshift=2.5cm]current page.center) {$\lozenge$};

  % Quote box
  \node[
    anchor=center,
    fill=white!10!truyenblue,
    text=white,
    draw=truyengold,
    line width=1pt,
    rounded corners=5pt,
    text width=13cm,
    align=center,
    inner sep=12pt,
    font=\itshape\large
  ] at ([yshift=-4.0cm]current page.center) {%
    ``The greatest revolution of our generation is the discovery\\
    that human beings, by changing the inner attitudes of their minds,\\
    can change the outer aspects of their lives.''\\[6pt]
    \normalfont\small\textcolor{truyengold}{--- William James}%
  };

  % Bottom tag
  \node[
    anchor=south,
    text=truyengold,
    font=\normalsize\bfseries
  ] at ([yshift=2.7cm]current page.south) {120 Truyện Song Ngữ · 12 Chương · Việt -- English};

\end{tikzpicture}
\end{titlepage}
\clearpage
